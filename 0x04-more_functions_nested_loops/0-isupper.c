#include "main.h"

/**
 * _isupper - checks for uppercase character
 * @b: function to parameter
 * Return: 1 on Success or 0 on failure
 */
int _isupper(int b)
{
	if (b >= 65 && b <= 90)
	{
	return (1);
	}
	return (0);
}
